Statische Bilder, werden 1:1 nach `/img/...` ausgeliefert (public/ ist der
Angular-Assets-Ordner). In Templates referenzieren als `img/<pfad>`.

- `avatar/` – Avatar-Auswahl bei der Registrierung (avatar01–06.svg, profile_blank.svg)
